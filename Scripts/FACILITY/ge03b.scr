printf("GE4b started");

FRAME player;   FRM_FindFrame(player, "_PLAYER_ 0");

OnSignal(69) {
    printf("GE4b received signal 69!");

    HUMAN_TurnAtNearestPlayer();
    // HUMAN_WeaponOnArm(true);
}